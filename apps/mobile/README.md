# Mobile application

The Expo/React Native application will be added after the web authentication and inventory workflows are stable. It will share domain types, validation, and Supabase access rules with the web application.
